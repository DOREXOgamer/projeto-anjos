version: '3.8'
services:
  db:
    image: mongo:6.0
    container_name: projeto_anjos_db
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
